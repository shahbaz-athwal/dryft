---
alwaysApply: true
---

After making code changes to any file, always run the lint command to fix any lint errors automatically:

```bash
bun run lint <path-to-file>
```

Do NOT ask for permission to run this command - just run it automatically after editing files.
